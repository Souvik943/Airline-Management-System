server:
  port: 8943

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airlinesystem?useSSL=false&serverTimezone=UTC
    username: root
    password: Ricky_943

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.MySQL8Dialect